si pas d'env faut rajouter :
    PWD=/home/picatrai/new-minishell
    SHLVL=1
    _=/usr/bin/env


mettre a jour l'env
    shell lvl
    dernier commande
    PWD
    exit status

bloquer quand on a trop de fichier ouvert

question

    changer le atoi

revoir le readline_heredoc et split

truc a faire bonus
    ~ et - pour cd

rajouter des messages d erreurs notament pour les erreures mallocs


faire dup2
faire cas avec 1 noeud
exit bultin