si pas d'env faut rajouter :
    PWD=/home/picatrai/new-minishell
    SHLVL=1
    _=/usr/bin/env


mettre a jour l'env
    shell lvl
    dernier commande
    PWD
    exit status

bloquer quand on a trop de fichier ouvert

question
    pourquoi dans l'ancien je prennais pas les tokens vides

    que ce passe t il si on change les droits de proc/self/stat

    changer le atoi

revoir le readline_heredoc